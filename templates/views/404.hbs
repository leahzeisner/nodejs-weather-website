<!DOCTYPE html>

<html>
    <header>
        <title>404</title>
        <link rel="icon" href="/images/weather.png" />
        <script src="./js/app.js"></script>
    </header>

    <body>
        <div class="main-content">
            {{>header}}
            <p>{{errorMessage}}</p>
        </div>
        
        {{>footer}}
    </body>
</html>